<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener Service</title>
    <style>
        * {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        table * {
            text-align: center;
            padding: 4px;
        }
    </style>
</head>

<body>
    <h1>URL Shortener</h1>
    <% if (locals.id) { %>
        <p>Generated URL is = <%= "http://localhost:8000/url/" +locals.id %>
        </p>
        <% } %>
            <form method="POST" action="/url">
                <label>Enter your url here</label>
                <input type="text" name="url" placeholder="https://example.com" required>
                <input type="submit" value="Generate">
            </form>
            <div>
                <% if (locals.urls) { %>
                    <table>
                        <thead>
                            <tr>
                                <th>Sno.</th>
                                <th>Short URL</th>
                                <th>Redirect URL</th>
                                <th>Clicks</th>
                            </tr>

                        </thead>
                        <tbody>
                            <% locals.urls.forEach((url,index)=> { %>
                                <tr>
                                    <td>
                                        <%= index+1 %>
                                    </td>
                                    <td>
                                        <%= "http://localhost:8000/url/" +url.short_url %>
                                    </td>
                                    <td>
                                        <%= url.url %>
                                    </td>
                                    <td>
                                        <%= url.times_visited.length %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                    <% } %>
            </div>
</body>

</html>